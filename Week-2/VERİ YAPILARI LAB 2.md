# **VERİ YAPILARI LAB 2**
1. Integer pointer ile değişkenin adresine erişip değerini yazdıran bir kod yazınız.
```c

```


2.  
  - Header dosyası kullanarak paylaşılan fonksiyon ve makroları içeren bir .h dosyası ile ilgili örnek kodunu yazın.
  - 2 boyutlu bir noktanın x ve y koordinatlarını tutan bir struct tanımlayın ve struct pointer kullanarak koordinatlara erişen kodunu yazın.
  - malloc fonksiyonu ile bir integer değişken için bellek ayırın ve ayırdığınız bu bellek alanına bir değer atayarak ekrana yazdıran kodunu yazın.
  - createNode fonksiyonu ile bir struct pointer oluşturun ve node->dataPtr ile struct içindeki veriye erişen kodunu yazın.
```c

```
```h

```


3.  
  - Tek bağlı liste (linked list) oluşturmak için NODE struct kullanın. 
  - Bellekte iki integer için yer ayırın (malloc kullanarak). 
  - İlk düğümü (node) ve ikinci düğümü (node->link) oluşturun. 
  - Her iki düğümün verilerini ekrana yazdıran kodunu yazın.
```c

```
```h

```


4.  
  - İki integer değişkeni karşılaştıran bir fonksiyon (compare) yazın.
  - Fonksiyon prototipi: int compare(void* ptr1, void* ptr2), ptr1 >= ptr2 ise +1, aksi durumda -1 döndürsün. 
  - larger isimli bir fonksiyon ile iki sayıyı karşılaştırıp büyük olan değeri döndüren kodu yazın. 
  - Ekrana büyük değeri yazdırın.
```c

```
```h

```


5.
  - İki float değişkeni karşılaştıran bir fonksiyon (compare) yazın. 
  - Fonksiyon prototipi: int compare(void* ptr1, void* ptr2). ptr1 >= ptr2 ise +1, aksi durumda -1 döndürsün. 
  - larger isimli bir fonksiyon ile iki float sayıyı karşılaştırıp büyük olan değeri döndüren kodu yazın. 
  - Ekrana büyük değeri 5.1 formatında yazdırın (virgülden sonra 1 basamak, 5 karakter genişlik).
```c

```
```h

```